# Contributing

Read the project contracts before changing behavior. Keep commits atomic and
conventional, keep machine output stable, and add regression proof for safety
or compatibility changes.

Run `just gate` before handing off a change. Do not add an upstream NetBird
dependency without checking its license boundary and recording the source and
version evidence.
